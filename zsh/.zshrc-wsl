# drop wsl cache, free up windows RAM
alias drop_cache="sudo sh -c \"echo 3 >'/proc/sys/vm/drop_caches' && swapoff -a && swapon -a && printf '\n%s\n' 'Ram-cache and Swap Cleared'"

alias cmd='cmd.exe'
alias shutdownwsl='cmd.exe /C "wsl --shutdown"'
